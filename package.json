{
  "name": "agent",
  "version": "0.0.1",
  "workspaces": ["packages/*"],
  "scripts": {
    "elysia-http:dev": "bun run --filter 'pkg-elysia' dev",
    "langchain:dev": "bun run --filter 'pkg-langchain' dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "^1.2.2"
  }
}
